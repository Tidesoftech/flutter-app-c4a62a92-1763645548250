name: premier_airport_transit_service
description: "Premier Airport Transit Service is a Wilmington, North Carolina-based private transportation company serving the Greater Wilmington region, with a"
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
